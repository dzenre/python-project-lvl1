name: Linter Check

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Install dependencies
        run: |
          pip install poetry
          
      - name: Run linter and pytest
        run: |
          make lint
